import { Text } from 'react-native';
import AppBars from '../../../components/AppBars';
import AppBarsSaleCost from '../../../components/AppBarSaleCost';

export default function () {
    return (
        <>
            <AppBarsSaleCost title="POS Pusrchase" />
        </>
    );
}
